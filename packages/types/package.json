{
  "name": "@repo/types",
  "version": "0.0.0",
  "description": "Shared TypeScript types for the Movie Explorer app",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc -b",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  }
} 